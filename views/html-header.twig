<!doctype html>
<!--[if lt IE 9]><html class="no-js no-svg ie lt-ie9 lt-ie8 lt-ie7" {{ site.language_attributes }}> <![endif]-->
<!--[if IE 9]><html class="no-js no-svg ie ie9 lt-ie9 lt-ie8" {{ site.language_attributes }}> <![endif]-->
<!--[if gt IE 9]><!--><html class="no-js no-svg" {{ site.language_attributes }}> <!--<![endif]-->
<head>
    <meta charset="{{ site.charset }}" />
    <meta name="description" content="{{ site.description }}">
    <link rel="stylesheet" href="{{ site.theme.link }}/css/reset.css" type="text/css" media="screen" />
    <link rel="stylesheet" href="{{ site.theme.link }}/css/style.css" type="text/css" media="screen" />
    <link rel="stylesheet" href="{{ site.theme.link }}/css/utilities.css" type="text/css" media="screen" />

    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="author" href="{{ site.theme.link }}/humans.txt" />
    <link rel="pingback" href="{{ site.pingback_url }}" />
    <link rel="profile" href="http://gmpg.org/xfn/11">
    {{function('wp_head')}}
    {{ dump() }}
    {# Style block used for global styles set on the Site Settings Options page. #}
    <style>
    
        /* Vars */
        .primary {
            --textcolor: {{ options.colours.primary.text_colour }};
            --bgcolor: {{ options.colours.primary.bg_colour }};
            color: var(--textcolor);
            background-color: var(--bgcolor);
        }
        .secondary {
            --textcolor: {{ options.colours.secondary.text_colour }};
            --bgcolor: {{ options.colours.secondary.bg_colour }};
            color: var(--textcolor);
            background-color: var(--bgcolor);
        }
        .tertiary {
            --textcolor: {{ options.colours.tertiary.text_colour }};
            --bgcolor: {{ options.colours.tertiary.bg_colour }};
            color: var(--textcolor);
            background-color: var(--bgcolor);
        }
        .quaternary {
            --textcolor: {{ options.colours.quaternary.text_colour }};
            --bgcolor: {{ options.colours.quaternary.bg_colour }};
            color: var(--textcolor);
            background-color: var(--bgcolor);
        }
        
        .wrapper {
            width: 100%;
            max-width: {{ options.max_width }}px;
            margin: 0 auto;
        }

    </style>
